#ifndef WINDOW_TEST
#define WINDOW_TEST

/**
 * @brief Displays a dialog on a device running Windows
 * 
 */
class WindowTest
{
    public:
    int x = 0;
    void show_dialog();

    private:
    int y = 0;
};

#endif
